apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-offboard.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-offboard.name" . }}
spec:
  rules:
    - host: dclaw-offboard.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-offboard.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
